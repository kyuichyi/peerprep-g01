services:
  collab-db:
    image: postgres:16-alpine
    container_name: peerprep_collab_db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: peerprep_collab_db
    ports:
      - "5434:5432"
    volumes:
      - ./src/models/init.sql:/docker-entrypoint-initdb.d/init.sql
      - collab_pgdata:/var/lib/postgresql/data
    networks:
      - peerprep-network

  collab-app:
    build: .
    container_name: peerprep_collab_app
    restart: always
    environment:
      PORT: 3004
      DATABASE_URL: postgresql://postgres:password123@collab-db:5432/peerprep_collab_db
      JWT_SECRET: ${JWT_SECRET:-your-super-secret-key-change-in-production}
      SERVICE_SECRET: ${SERVICE_SECRET:-peerprep-internal-service-key-2026}
      USER_SERVICE_URL: http://user-app:3001
      FRONTEND_URL: http://localhost
    ports:
      - "3004:3004"
    volumes:
      - .:/app
      - /app/node_modules
    depends_on:
      - collab-db
    networks:
      - peerprep-network

networks:
  peerprep-network:
    external: true

volumes:
  collab_pgdata:
